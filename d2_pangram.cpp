//check pangram or not
#include<stdio.h>
#include<ctype.h>
#include<string.h>
int main()
{
	char st[50];
	gets(st);
	int i,len,count;
	len=strlen(st);
	for(i=0;i<len;i++)
	{ 
	 
	}
	return 0;
}
